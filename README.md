# Yuan Says AI

Yuan 的个人网站，记录 AI、产品与独立创造。

首页使用 Canvas 实现可交互粒子场：鼠标靠近会吸引粒子，按下会产生排斥效果，并兼顾触控设备与减少动态效果偏好。

## 本地开发

```bash
npm install
npm run dev
```

访问 `http://localhost:3000`。

## 构建

```bash
npm run build
```

静态产物输出到 `out/`，可直接部署到 Cloudflare Pages。
